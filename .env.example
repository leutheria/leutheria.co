# Web3Forms access key — free, no account required.
# Get one at https://web3forms.com by entering the destination inbox;
# the key is mailed to you instantly.
#
# This key is public by design (it only allows POSTing to the inbox it is bound
# to), which is why it is a NEXT_PUBLIC_ variable and safe to expose in the client bundle.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
